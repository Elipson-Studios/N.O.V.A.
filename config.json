{
    "training": {
        "epochs": 100,
        "batch_size": 32,
        "learning_rate": 0.001,
        "optimizer": "adam",
        "loss": "categorical_crossentropy",
        "metrics": ["accuracy"]
    },
    "instructions": {
        "responseFormat": "Do not add commands to the response, just the command."
    },
    "commands": {
        "prefix": "!"
    },
    "restrictions": {
        "allowSearch": false,
        "allowCustomCommands": false,
        "silentMode": false
    }
}